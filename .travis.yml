language: go

go:
  - 1.10.x
  - 1.11.x

install:
  - export PATH=${PATH}:${HOME}/gopath/bin
  - go get -v github.com/golang/lint/golint

before_script:
  - go vet ./...
  - golint .

script:
  - go test -v ./...
  - go test -v -test.race ./...
